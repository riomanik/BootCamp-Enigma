let getRepository,UserInfo;_dad‍.x([["default",()=>UserInfoRepository]]);_dad‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_dad‍.w("../model/userInfo.model",[["default",["UserInfo"],function(v){UserInfo=v}]]);


               class UserInfoRepository {
  userInfoRepository() {
    return getRepository(UserInfo);
  }

  async findAllUserInfo() {
    const userInfo = await this.userInfoRepository().find();
    return userInfo;
  }

  async findOne(id) {
    const userInfo = await this.userInfoRepository().find({
      where: { idUserInfo: id }
    });
    return userInfo;
  }
}
