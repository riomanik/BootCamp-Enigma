let getRepository,Category;_8be‍.x([["default",()=>CategoryRepository]]);_8be‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_8be‍.w("../model/category.model",[["default",["Category"],function(v){Category=v}]]);


               class CategoryRepository {
  categoryRepository() {
    return getRepository(Category);
  }

  async findAllCategory() {
    const category = await this.categoryRepository().find();
    return category;
  }

  async findOne(id) {
    const category = await this.categoryRepository().find({
      where: { id: id }
    });
    return category;
  }

  // async findAllCategoryProduct() {
  //   const category = await this.categoryRepository().find({
  //     relations: ["product"]
  //   });
  //   return category;
  // }

  async saveCategory(category) {
    const saveCategory = await this.categoryRepository().save(category);
    return saveCategory;
  }
  async updateCategory(category) {
    const updateCategory = await this.categoryRepository().save(category);
    return updateCategory;
  }
  async deleteCategory(category) {
    const deleteCategory = await this.categoryRepository().delete(category);
    _8be‍.g.console.log(JSON.stringify(deleteCategory) + "ini deleted");
    console.log("deleted");
    return deleteCategory;
  }
  // async disableCategory(category) {
  //   let categoryWantDisable = await {
  //     id: category[0].id,
  //     categoryName: category[0].categoryName,
  //     status: {
  //       idStatus: 4
  //     }
  //   };
  //   const deleteCategory = await this.categoryRepository().save(
  //     categoryWantDisable
  //   );
  //   return deleteCategory;
  // }
}
