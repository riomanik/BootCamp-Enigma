export default class Product {
  constructor(productId, productName, price, status) {
    this.productId = productId;
    this.productName = productName;
    this.price = price;
    this.status = status;
  }
}
