let getRepository,User;_5d3‍.x([["default",()=>UserRepository]]);_5d3‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_5d3‍.w("../model/user.model",[["default",["User"],function(v){User=v}]]);


               class UserRepository {
  userRepository() {
    return getRepository(User);
  }

  async findAllUser() {
    const user = await this.userRepository().find();
    return user;
  }

  async findOne(id) {
    const user = await this.userRepository().find({
      where: { idUser: id }
    });
    return user;
  }

  async saveUser(user) {
    const saveUser = await this.userRepository().save(user);
    return saveUser;
  }
}
