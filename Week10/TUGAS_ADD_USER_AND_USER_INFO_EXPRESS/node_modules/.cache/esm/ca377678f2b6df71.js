let getRepository,UserInfo;_f2b‍.x([["default",()=>UserInfoRepository]]);_f2b‍.w("typeorm",[["getRepository",["getRepository"],function(v){getRepository=v}]]);_f2b‍.w("../model/userInfo.model",[["default",["UserInfo"],function(v){UserInfo=v}]]);


               class UserInfoRepository {
  userInfoRepository() {
    return getRepository(UserInfo);
  }

  async findAllUserInfo() {
    const userInfo = await this.userInfoRepository().find();
    return userInfo;
  }

  async findOne(id) {
    const userInfo = await this.userInfoRepository().find({
      where: { idUserInfo: id }
    });
    return userInfo;
  }

  async saveUserInfo(userInfo) {
    const saveUserInfo = await this.userInfoRepository().save(userInfo);
    return saveUserInfo;
  }
}
